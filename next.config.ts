import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,  // Aktiviert den strengen Modus
  swcMinify: true,        // Aktiviert schnelles Minifizieren mit SWC
};

export default nextConfig;
